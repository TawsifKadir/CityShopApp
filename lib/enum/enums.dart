/// Device
enum DeviceType { mobile, tablet, web, mac, windows, linux, fuchsia }

/// HTTP
enum Header { http, auth }

/// Screen Pref

enum ImageType { memory, network }

enum ComplainListPref { my_regular_complain, complain_regular, my_complain_history, appeal_regular }

enum BlackListPref { blacklist, requested_blacklist }

enum ActionViewPref { list_item, details_view }

enum ActionTypePref { complain, appeal }

enum PasswordPref { changed, updated }

enum OtpPref { create_account, forgot_password }

enum CalenderPref { task }

enum NoDataPref { complains, history, blacklist, branch_officers }

enum ButtonVariant { primary, secondary }
